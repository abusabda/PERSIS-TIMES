name: myhisab
description: Astronomical calculations for Islamic prayer times, qibla direction, hijri calendar, and eclipses.
version: 1.0.0
repository: https://github.com/abusabda/PERSIS-TIMES
homepage: https://github.com/abusabda/PERSIS-TIMES

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  decimal: ^3.2.4
  # path: ^1.8.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0

  dev_dependencies:
  test: ^1.25.0
